spring:
  application:
    name: command-side-blog
  cloud:
    config:
      enabled: true
      uri: ${vcap.services.config-server.credentials.uri:http://localhost:8888}
      
---

spring:
  profiles: docker
  cloud:
     config:
        uri: http://192.168.99.100:8888
        fail-fast: true