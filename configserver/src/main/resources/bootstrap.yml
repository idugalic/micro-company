spring:
  application:
    name: configserver
  cloud:
    config:
      enabled: true
      server:
        git:
          uri: https://bitbucket.org/idugalic_microservices_lab/micro-company-config.git
    
security:
  user:
    password: password
    
server:
  port: 8888 