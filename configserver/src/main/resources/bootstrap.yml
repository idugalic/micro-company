spring:
  application:
    name: configserver
  cloud:
    config:
      enabled: true
      server:
        git:
          uri: https://github.com/idugalic/micro-company-config.git
          default-label: swarm
    
security:
  user:
    password: password
    
server:
  port: 8888 